{
  "name": "saucedemo-playwright",
  "version": "1.0.0",
  "description": "Playwright E2E test suite against SauceDemo",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "report": "npx playwright show-report",
    "review": "node scripts/review-report.mjs --out playwright-report/review.md --json playwright-report/review.json",
    "review:test": "node --test scripts/review-report.test.mjs",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.52.0",
    "@types/node": "^25.9.1",
    "eslint": "^10.4.1",
    "eslint-plugin-playwright": "^2.10.4",
    "typescript": "^5.4.0",
    "typescript-eslint": "^8.60.1"
  }
}
